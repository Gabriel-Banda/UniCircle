// UniCircle — Supabase project config.
// Create a free project at https://supabase.com, run supabase/schema.sql
// in its SQL editor, then paste your project's URL and anon (public) key
// below. The anon key is safe to expose client-side — row level security
// in schema.sql is what actually protects the data.

export const SUPABASE_URL = "https://YOUR-PROJECT.supabase.co";
export const SUPABASE_ANON_KEY = "YOUR-ANON-KEY";
